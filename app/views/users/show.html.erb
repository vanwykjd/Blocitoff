<div class="row">
    <div class="col-md-8">
        <div class="media">
        <br />
            <div class="media-body">
                <div class="page-header">
                    <h1 class="text-center"><%= current_user.username.capitalize! %>'s<small>To-Do List</small></h1><br />
                </div>
            <div class='js-items'>
                <h3><%= render @user.items %></h3>
            </div>
           
            <% if current_user %>
                <div class="new-item">
                    <%= render 'items/form', item: Item.new, user: @user %>
                </div>
            <% end %>

            </div>
        </div>
    </div>
</div>